{
  "name": "ia-colombia-immersive-static-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Experiencia web inmersiva bilingüe de IA Colombia con SEO estático, rutas indexables y contenido heredado de ia-col.com.",
  "scripts": {
    "validate:site": "node scripts/validate-immersive.mjs",
    "validate:assets": "node scripts/validate-immersive.mjs --assets-only",
    "validate": "npm run validate:site",
    "build": "node scripts/build-immersive.mjs",
    "validate:dist": "node scripts/validate-dist-immersive.mjs",
    "check": "npm run validate && npm run build && npm run validate:dist",
    "preview": "npm run build && npx serve dist",
    "test": "npm run check"
  },
  "engines": {
    "node": ">=20"
  }
}